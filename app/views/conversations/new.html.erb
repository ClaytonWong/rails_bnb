<%= link_to 'Back to listing', listing_path(params[:listing_id]) %>

<%= form_with(model: @conversation, local: true) do |form| %>
  <% if @conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
      <% @conversation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :listing_id, value: params[:listing_id] %>
  <%= form.hidden_field :guest_id, value: params[:guest_id] %>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>